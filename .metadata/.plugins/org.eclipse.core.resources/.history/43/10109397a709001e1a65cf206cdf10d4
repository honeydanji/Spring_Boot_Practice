
public class 설명서 {

	/*
	 * 1. Web Browser : 시작
	 * 2. Contoroller : 웹 브라우저의 요청을 처리한다. 이후에 Service를 호출한다.
	 * 3. Service : 비지니스 로직을 수행 한다. 데이터베이스에 접근하는 DAO를 이용해서 결과값을 받아 온다.
	 * 4. DAO : DAO는 데이터베이스에 접속하여 비즈니스 로직 실행에 필요한 쿼리를 호출한다.
	 * 5. DB : 알맞은 쿼리를 실행하고 결과값을 반환한다.
	 * 
	 * domain = VO = DTO (Data Transfer Object) : 각 계층이 데이터를 주고 받을 때 사용하는 객체를 말한다.
	 * 
	 * */ 
}
